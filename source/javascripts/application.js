//= require jquery
//= require bootstrap
//= require_tree .

//fetch("http://api.openweathermap.org/data/2.5/forecast?id=524901&APPID=23540a6b8724dda669b1bd5ae46534fc")
    //.then(response => response.json())
    //.then((data) => {
      //document.querySelectorAll("li").forEach(listItem => listItem.remove());
      //data.words.forEach((word) => {
        //const item = `<li>${word}</li>`;
        //.insertAdjacentHTML("beforeend", item);
      //});
    //});
//});